# @isletme/ui

Shared React component library (web/DOM targets — used by `apps/web`).

Currently contains a single placeholder `Button` component to establish the
package structure. Add real design-system components here as the product
grows.
