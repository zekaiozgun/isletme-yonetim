/**
 * @isletme/ui
 *
 * Shared React component library (web). Intentionally minimal —
 * this establishes the package structure for future design-system work.
 */

export { Button } from './Button';
export type { ButtonProps } from './Button';
