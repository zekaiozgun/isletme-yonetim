# @isletme/shared

Framework-agnostic utilities/helpers shared by `apps/web`, `apps/mobile`, and
`apps/api`. Keep this package free of React/React Native/Nest-specific code —
those belong in `@isletme/ui` or the individual apps.
