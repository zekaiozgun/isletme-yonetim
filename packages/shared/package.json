{
  "name": "@isletme/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utilities/helpers for İşletme Yönetim",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@isletme/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
