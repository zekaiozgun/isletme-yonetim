{
  "name": "@isletme/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for İşletme Yönetim",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo",
    "generate:api-types": "openapi-typescript openapi.json -o src/generated/api.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.5.2",
    "typescript": "^5.7.2"
  }
}
